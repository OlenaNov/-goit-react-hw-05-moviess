import styled from "styled-components";

export const Title = styled.h1`
    visibility: hidden;
`;

export const List = styled.ul`
    list-style: none;
    display: flex;
    gap: 28px;
    flex-wrap: wrap;
`;
